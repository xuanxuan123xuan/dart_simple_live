name: 构建 Android TV APK

on:
  workflow_dispatch:
    inputs:
      version:
        description: "可选版本号（如 1.13）；留空使用 pubspec 当前版本"
        required: false
        type: string

permissions:
  contents: read

env:
  TV_FLUTTER_VERSION: "3.41.x"

jobs:
  build:
    name: 构建 Android TV APK 与 AAB
    runs-on: ubuntu-22.04
    timeout-minutes: 60
    steps:
      - name: 检出源码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: "17"
          cache: gradle

      - name: 配置 Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.TV_FLUTTER_VERSION }}
          cache: true

      - name: 准备 Android TV 签名（可选）
        shell: bash
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
          STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
        run: |
          if [[ -n "$KEYSTORE_BASE64" && -n "$STORE_PASSWORD" && \
                -n "$KEY_PASSWORD" && -n "$KEY_ALIAS" ]]; then
            printf '%s' "$KEYSTORE_BASE64" | openssl base64 -d -A \
              > simple_live_tv_app/android/release-keystore.jks
            printf 'storeFile=../release-keystore.jks\nstorePassword=%s\nkeyPassword=%s\nkeyAlias=%s\n' \
              "$STORE_PASSWORD" "$KEY_PASSWORD" "$KEY_ALIAS" \
              > simple_live_tv_app/android/key.properties
          else
            echo "::warning::签名 secrets 不完整，将使用 debug fallback 签名。"
          fi

      - name: 恢复 TV 依赖包
        working-directory: simple_live_tv_app
        run: flutter pub get

      - name: 构建 TV 通用 APK
        working-directory: simple_live_tv_app
        run: flutter build apk --release --no-pub

      - name: 构建 TV 分架构 APK
        working-directory: simple_live_tv_app
        run: flutter build apk --release --split-per-abi --no-pub

      - name: 构建 TV AAB
        working-directory: simple_live_tv_app
        run: flutter build appbundle --release --no-pub

      - name: 上传 APK
        uses: actions/upload-artifact@v4
        with:
          name: android-tv-apk
          path: simple_live_tv_app/build/app/outputs/flutter-apk/*.apk
          if-no-files-found: error
          retention-days: 14

      - name: 上传 AAB
        uses: actions/upload-artifact@v4
        with:
          name: android-tv-aab
          path: simple_live_tv_app/build/app/outputs/bundle/release/*.aab
          if-no-files-found: error
          retention-days: 14
